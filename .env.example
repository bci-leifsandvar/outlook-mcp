# Fork Notice: This is a fork. Upstream certifications are not valid.

# Microsoft Azure App Registration Credentials
# Get these from Azure Portal > App Registrations > Your App

# Application (client) ID from Azure Portal
MS_CLIENT_ID=your-client-id-here

# Client secret VALUE (not the secret ID) from Azure Portal > Certificates & secrets
MS_CLIENT_SECRET=your-client-secret-here

# Optional: Enable test mode with mock data (true/false)
USE_TEST_MODE=false

# Secure prompting mode (default: true, set to false to disable explicit user confirmation for sensitive actions)
SECURE_PROMPT_MODE=true

# Restrict Microsoft Graph API scopes (comma-separated, only allowed scopes will be used)
# Example: OUTLOOK_SCOPES="Mail.Read,User.Read,Calendars.Read"
OUTLOOK_SCOPES=

# Optional: Override Graph API endpoint (for mock server testing)
# GRAPH_API_ENDPOINT=http://localhost:4000/v1.0/